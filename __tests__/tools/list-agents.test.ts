import { describe, it } from 'vitest';

describe('list_agents', () => {
  it.todo('returns agents for a valid org');
  it.todo('returns empty list when org has no agents');
  it.todo('returns auth error for invalid token');
});
